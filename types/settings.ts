export interface SettingsData {
  company_name: string;
  max_search_results: string;
  score_active_single_penalty: string;
  score_active_multiple_penalty: string;
  score_debt_moderate_penalty: string;
  score_debt_high_penalty: string;
  score_debt_very_high_penalty: string;
  score_recent_penalty: string;
}
